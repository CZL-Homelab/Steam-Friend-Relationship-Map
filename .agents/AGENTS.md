# Antigravity Rules

## Branching & Development Workflow
- **Never develop directly on the `dev-base` branch / 绝不直接在 `dev-base` 分支进行开发**:
  All development work must be done on specific feature branches (e.g., `dev-N` or descriptive branches). Direct commits and development on the `dev-base` branch are strictly prohibited.
